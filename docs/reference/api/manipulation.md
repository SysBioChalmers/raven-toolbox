# `raven_toolbox.manipulation`

Generic {class}`cobra.Model` structural transforms that cobrapy does not cover cleanly:
reaction building from equations, batch GPR / bound changes, irreversibility splitting,
isozyme expansion, compartment merge / copy, and model merging by name.

```{eval-rst}
.. automodule:: raven_toolbox.manipulation
   :members:
   :imported-members:
```

## Compartment helpers

```{eval-rst}
.. automodule:: raven_toolbox.manipulation.compartments
   :members:
```
