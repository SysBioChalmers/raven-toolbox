# `raven_toolbox.comparison`

Structural and functional comparison across multiple models — tidy DataFrames of
reaction / metabolite / gene / subsystem presence, pairwise Jaccard, and an optional
`check_tasks` pass/fail matrix.

```{eval-rst}
.. automodule:: raven_toolbox.comparison
   :members:
   :imported-members:
```
