# `raven_toolbox.tasks`

Metabolic task definition, parsing, and checking — the task-list file format, running tasks
against a model, and finding the reactions a model must use to satisfy a task list (the input
for (f)tINIT's task layer).

```{eval-rst}
.. automodule:: raven_toolbox.tasks
   :members:
   :imported-members:
```
